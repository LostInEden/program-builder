// The coach's own football brain, as exported from his ChatGPT history
// (docs/coach-workspace/CHATGPT-EXPORT-PROMPT.md). Server-only: it is added
// to every model call's standing instructions and never shipped to the
// browser.
//
// Paste the "COACH KNOWLEDGE" section of his export between the backticks.
// Keep it to what he actually said — his rules, words and philosophy — not
// anyone's summary of what a defense "should" do. Empty = the model works from
// the app's saved data alone.

export const COACH_KNOWLEDGE = ``;
